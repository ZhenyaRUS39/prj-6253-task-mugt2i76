# Plan: prj-6253-task-mugt2i76

**Project:** `PRJ-3635`  
**Task ID:** `task-mugt2i76`  
**Repo:** `prj-6253-task-mugt2i76`  

## Summary

Декомпозиция задачи "Сделать парсер логов nginx v2": CLI-утилита на Python с парсингом access.log (combined формат), агрегацией статистики и экспортом в JSON/CSV.

## Subtasks

### 1. Инициализировать структуру репозитория

- **ID:** `sub-1`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Создать структуру проекта: pyproject.toml с зависимостями (pytest, click), README.md с описанием, директории src/parser_nginx/ и tests/. Добавить .gitignore для Python.
- **Test plan:** Файлы pyproject.toml, README.md, .gitignore существуют; python -m pip install -e . завершается без ошибок.

### 2. Реализовать парсер строки access.log

- **ID:** `sub-2`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Реализовать модуль log_parser.py с функцией parse_line(line: str) -> LogEntry: парсинг combined-формата nginx через regex (IP, timestamp, метод, путь, статус, размер, referer, user-agent). Поддержка пустых/битых строк без падения.
- **Test plan:** Unit-тесты: pytest tests/test_log_parser.py зелёные (валидная строка, пустая, битая, кириллица в UA).
- **Dependencies:** `sub-1`

### 3. Реализовать агрегатор статистики

- **ID:** `sub-3`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Модуль stats.py: класс StatisticsCollector с методами add(entry), total_requests(), by_status(), top_paths(n), top_ips(n), by_method(), avg_response_size(). Хранение через collections.Counter.
- **Test plan:** pytest tests/test_stats.py: добавление 100 записей, проверка счётчиков и top-N списков.
- **Dependencies:** `sub-2`

### 4. Реализовать CLI и экспорт JSON/CSV

- **ID:** `sub-4`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Модуль cli.py на click: команда parser-nginx parse <file> [--top N] [--format json|csv] [--output PATH]. Чтение файла через streaming (без полной загрузки в память для больших логов), вывод статистики.
- **Test plan:** CLI тест: parser-nginx parse tests/fixtures/sample.log --top 5 --format json возвращает валидный JSON с ожидаемыми ключами.
- **Dependencies:** `sub-3`

### 5. Написать интеграционные тесты и фикстуры

- **ID:** `sub-5`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Подготовить tests/fixtures/sample.log (50+ записей разных статусов/методов) и tests/test_integration.py: полный pipeline от файла до вывода. Покрыть edge cases: пустой файл, файл с 1М строк (smoke-тест производительности).
- **Test plan:** pytest tests/test_integration.py все тесты зелёные; smoke-тест 100k строк отрабатывает < 5 сек.
- **Dependencies:** `sub-4`


---
*Generated by Hermes Orchestrator*
